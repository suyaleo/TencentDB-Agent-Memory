# Third-party notices

This project retains all upstream source notices and dependency license metadata. Important components include React, Vite, Hono, Graphology, Sigma.js, SQLite and sqlite-vec, the Model Context Protocol Python SDK, and CodeGraph-derived code acknowledged in `README.md`.

Before any public binary redistribution, generate a dependency license inventory from the locked Node/Python dependencies and preserve every required notice. This file does not relicense third-party works.
